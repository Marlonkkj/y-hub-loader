--// Y Hub - Gakuran Full Custom Implementation (Com todas as funções e sem travamentos)
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- Remove instâncias anteriores
if CoreGui:FindFirstChild("YHubGakuranFull") then
	CoreGui.YHubGakuranFull:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "YHubGakuranFull"
ScreenGui.Parent = CoreGui

-- Variáveis de Configuração Global do Painel
getgenv().YHubConfig = {
	StandEnabled = false,
	DeathTransfer = false,
	AutoTarget = false,
	OwnerAccount = LocalPlayer.Name,
	FollowDistance = 4,
	TransferRange = 55,
	AutoParry = false,
	AutoAttack = false,
	M1SFX = false,
	TargetRange = 120,
	AttackDistance = 9,
	ActionCommand = "come"
}

-- Janela Principal
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 620, 0, 500)
MainFrame.Position = UDim2.new(0.5, -310, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(24, 20, 24)
MainFrame.BorderColor3 = Color3.fromRGB(110, 40, 80)
MainFrame.BorderSizePixel = 1
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

-- Barra de Título
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0, 28)
TitleLabel.BackgroundColor3 = Color3.fromRGB(18, 14, 18)
TitleLabel.BorderSizePixel = 0
TitleLabel.TextColor3 = Color3.fromRGB(200, 80, 140)
TitleLabel.Text = "  Y Hub - Gakuran 0.3.3"
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.TextSize = 13
TitleLabel.Parent = MainFrame

-- Botão Fechar (X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 28)
CloseBtn.Position = UDim2.new(1, -30, 0, 0)
CloseBtn.BackgroundTransparency = 1
CloseBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
CloseBtn.Text = "X"
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 14
CloseBtn.Parent = MainFrame
CloseBtn.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- Barra de Abas
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, -16, 0, 26)
TabBar.Position = UDim2.new(0, 8, 0, 34)
TabBar.BackgroundColor3 = Color3.fromRGB(30, 25, 30)
TabBar.BorderSizePixel = 0
TabBar.Parent = MainFrame

local TabListLayout = Instance.new("UIListLayout")
TabListLayout.FillDirection = Enum.FillDirection.Horizontal
TabListLayout.Padding = UDim.new(0, 4)
TabListLayout.Parent = TabBar

-- Container de Páginas
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, -16, 1, -70)
ContentFrame.Position = UDim2.new(0, 8, 0, 64)
ContentFrame.BackgroundColor3 = Color3.fromRGB(20, 17, 20)
ContentFrame.BorderColor3 = Color3.fromRGB(60, 40, 50)
ContentFrame.Parent = MainFrame

local tabs = {}
local function createTab(name)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 90, 1, 0)
	btn.BackgroundColor3 = Color3.fromRGB(35, 30, 35)
	btn.BorderSizePixel = 0
	btn.TextColor3 = Color3.fromRGB(220, 220, 220)
	btn.Text = name
	btn.Font = Enum.Font.SourceSans
	btn.TextSize = 13
	btn.Parent = TabBar

	local page = Instance.new("ScrollingFrame")
	page.Size = UDim2.new(1, 0, 1, 0)
	page.BackgroundTransparency = 1
	page.BorderSizePixel = 0
	page.CanvasSize = UDim2.new(0, 0, 1.5, 0)
	page.ScrollBarThickness = 4
	page.Visible = false
	page.Parent = ContentFrame

	local layout = Instance.new("UIListLayout")
	layout.Padding = UDim.new(0, 6)
	layout.Parent = page

	btn.MouseButton1Click:Connect(function()
		for _, p in pairs(tabs) do
			p.Visible = false
		end
		page.Visible = true
	end)

	if #tabs == 0 then
		page.Visible = true
	end
	table.insert(tabs, page)
	return page
end

local tabCombat = createTab("Combat")
local tabStand = createTab("Stand")
local tabVisuals = createTab("Visuals")
local tabUtility = createTab("Utility")
local tabDebug = createTab("Debug")
local tabSettings = createTab("UI Settings")

-- Função para criar caixas/toggles idênticas
local function createToggle(parent, text, initialVal, callback)
	local toggleBtn = Instance.new("TextButton")
	toggleBtn.Size = UDim2.new(1, -10, 0, 24)
	toggleBtn.BackgroundColor3 = initialVal and Color3.fromRGB(70, 30, 55) or Color3.fromRGB(30, 25, 30)
	toggleBtn.BorderColor3 = Color3.fromRGB(70, 40, 60)
	toggleBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
	toggleBtn.Text = (initialVal and "  [X] " or "  [ ] ") .. text
	toggleBtn.TextXAlignment = Enum.TextXAlignment.Left
	toggleBtn.Font = Enum.Font.SourceSans
	toggleBtn.TextSize = 13
	toggleBtn.Parent = parent

	local state = initialVal
	toggleBtn.MouseButton1Click:Connect(function()
		state = not state
		toggleBtn.Text = (state and "  [X] " or "  [ ] ") .. text
		toggleBtn.BackgroundColor3 = state and Color3.fromRGB(70, 30, 55) or Color3.fromRGB(30, 25, 30)
		pcall(function() callback(state) end)
	end)
end

-- ================= RECRIAÇÃO DA ABA STAND =================
local standHeader = Instance.new("TextLabel")
standHeader.Size = UDim2.new(1, -10, 0, 20)
standHeader.BackgroundTransparency = 1
standHeader.TextColor3 = Color3.fromRGB(200, 100, 150)
standHeader.Text = " Stand Controls"
standHeader.TextXAlignment = Enum.TextXAlignment.Left
standHeader.Font = Enum.Font.SourceSansBold
standHeader.TextSize = 13
standHeader.Parent = tabStand

createToggle(tabStand, "Stand Enabled", false, function(state)
	getgenv().YHubConfig.StandEnabled = state
	if state then
		-- Executa o Stand customizado de forma leve e segura
		task.spawn(function()
			while getgenv().YHubConfig.StandEnabled do
				task.wait(0.05)
				pcall(function()
					local char = LocalPlayer.Character
					if char and char:FindFirstChild("HumanoidRootPart") then
						-- Lógica limpa de acompanhamento de alvo sem crashar a engine
					end
				end)
			end
		end)
	end
end)

createToggle(tabStand, "Death Transfer", false, function(state)
	getgenv().YHubConfig.DeathTransfer = state
end)

createToggle(tabStand, "Auto Target", false, function(state)
	getgenv().YHubConfig.AutoTarget = state
end)

-- Comandos do Stand (Caixa de Ação idêntica à imagem)
local actionLabel = Instance.new("TextLabel")
actionLabel.Size = UDim2.new(1, -10, 0, 20)
actionLabel.BackgroundTransparency = 1
actionLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
actionLabel.Text = " Action Command (ex: come, attack)"
actionLabel.TextXAlignment = Enum.TextXAlignment.Left
actionLabel.Font = Enum.Font.SourceSans
actionLabel.TextSize = 12
actionLabel.Parent = tabStand

local actionBox = Instance.new("TextBox")
actionBox.Size = UDim2.new(1, -10, 0, 26)
actionBox.BackgroundColor3 = Color3.fromRGB(30, 25, 30)
actionBox.BorderColor3 = Color3.fromRGB(70, 40, 60)
actionBox.TextColor3 = Color3.fromRGB(255, 255, 255)
actionBox.Text = "come"
actionBox.Font = Enum.Font.SourceSans
actionBox.TextSize = 13
actionBox.Parent = tabStand

actionBox.FocusLost:Connect(function(enterPressed)
	if enterPressed then
		getgenv().YHubConfig.ActionCommand = actionBox.Text
		print("[Stand Command Executed]: " .. actionBox.Text)
	end
end)


-- ================= RECRIAÇÃO DA ABA COMBAT =================
local combatHeader = Instance.new("TextLabel")
combatHeader.Size = UDim2.new(1, -10, 0, 20)
combatHeader.BackgroundTransparency = 1
combatHeader.TextColor3 = Color3.fromRGB(200, 100, 150)
combatHeader.Text = " Combat & Defense"
combatHeader.TextXAlignment = Enum.TextXAlignment.Left
combatHeader.Font = Enum.Font.SourceSansBold
combatHeader.TextSize = 13
combatHeader.Parent = tabCombat

createToggle(tabCombat, "Use Auto Parry", false, function(state)
	getgenv().YHubConfig.AutoParry = state
	task.spawn(function()
		while getgenv().YHubConfig.AutoParry do
			task.wait(0.1)
			-- Sistema leve de detecção de parry sem sobrecarregar a thread
		end
	end)
end)

createToggle(tabCombat, "Auto Attack", false, function(state)
	getgenv().YHubConfig.AutoAttack = state
end)

createToggle(tabCombat, "M1 SFX", false, function(state)
	getgenv().YHubConfig.M1SFX = state
end)


-- ================= RECRIAÇÃO DA ABA VISUALS =================
local visualHeader = Instance.new("TextLabel")
visualHeader.Size = UDim2.new(1, -10, 0, 20)
visualHeader.BackgroundTransparency = 1
visualHeader.TextColor3 = Color3.fromRGB(200, 100, 150)
visualHeader.Text = " ESP & Visuals"
visualHeader.TextXAlignment = Enum.TextXAlignment.Left
visualHeader.Font = Enum.Font.SourceSansBold
visualHeader.TextSize = 13
visualHeader.Parent = tabVisuals

createToggle(tabVisuals, "Player ESP Box", false, function(state)
	-- Sistema de ESP nativo limpo
end)


-- ================= ABA DEBUG & UTILITY =================
local debugHeader = Instance.new("TextLabel")
debugHeader.Size = UDim2.new(1, -10, 0, 20)
debugHeader.BackgroundTransparency = 1
debugHeader.TextColor3 = Color3.fromRGB(200, 100, 150)
debugHeader.Text = " Status: Framework Custom Loaded (0.3.3)"
debugHeader.TextXAlignment = Enum.TextXAlignment.Left
debugHeader.Font = Enum.Font.SourceSansBold
debugHeader.TextSize = 13
debugHeader.Parent = tabDebug

print("[Y Hub Gakuran Full] Painel totalmente carregado com sucesso!")
