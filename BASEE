--// Number One Hub - Custom Gakuran Design (Ultra Otimizado e Seguro)
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- Remove instâncias anteriores para evitar duplicatas
if CoreGui:FindFirstChild("NumberOneHub") then
	CoreGui.NumberOneHub:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NumberOneHub"
ScreenGui.Parent = CoreGui

-- Janela Principal (Tamanho e Cores exatas do modelo)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 620, 0, 500)
MainFrame.Position = UDim2.new(0.5, -310, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(24, 20, 24)
MainFrame.BorderColor3 = Color3.fromRGB(110, 40, 80)
MainFrame.BorderSizePixel = 1
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

-- Barra de Título com o nome Number One
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0, 28)
TitleLabel.BackgroundColor3 = Color3.fromRGB(18, 14, 18)
TitleLabel.BorderSizePixel = 0
TitleLabel.TextColor3 = Color3.fromRGB(200, 80, 140)
TitleLabel.Text = "  Number One - 0.3.3"
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.TextSize = 13
TitleLabel.Parent = MainFrame

-- Botão Fechar (X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 28)
CloseBtn.Position = UDim2.new(1, -30, 0, 0)
CloseBtn.BackgroundTransparency = 1
CloseBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
CloseBtn.Text = "X"
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 14
CloseBtn.Parent = MainFrame
CloseBtn.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- Barra de Abas Superiores (Combat, Stand, Visuals, Utility, Debug, UI Settings)
local TabBar = Instance.new("Frame")
TabBar.Size = UDim2.new(1, -16, 0, 26)
TabBar.Position = UDim2.new(0, 8, 0, 34)
TabBar.BackgroundColor3 = Color3.fromRGB(30, 25, 30)
TabBar.BorderSizePixel = 0
TabBar.Parent = MainFrame

local TabListLayout = Instance.new("UIListLayout")
TabListLayout.FillDirection = Enum.FillDirection.Horizontal
TabListLayout.Padding = UDim.new(0, 4)
TabListLayout.Parent = TabBar

-- Container Principal das Páginas
local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, -16, 1, -70)
ContentFrame.Position = UDim2.new(0, 8, 0, 64)
ContentFrame.BackgroundColor3 = Color3.fromRGB(20, 17, 20)
ContentFrame.BorderColor3 = Color3.fromRGB(60, 40, 50)
ContentFrame.Parent = MainFrame

local tabs = {}
local function createTab(name)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 90, 1, 0)
	btn.BackgroundColor3 = Color3.fromRGB(35, 30, 35)
	btn.BorderSizePixel = 0
	btn.TextColor3 = Color3.fromRGB(220, 220, 220)
	btn.Text = name
	btn.Font = Enum.Font.SourceSans
	btn.TextSize = 13
	btn.Parent = TabBar

	local page = Instance.new("ScrollingFrame")
	page.Size = UDim2.new(1, 0, 1, 0)
	page.BackgroundTransparency = 1
	page.BorderSizePixel = 0
	page.CanvasSize = UDim2.new(0, 0, 1.4, 0)
	page.ScrollBarThickness = 4
	page.Visible = false
	page.Parent = ContentFrame

	local layout = Instance.new("UIListLayout")
	layout.Padding = UDim.new(0, 6)
	layout.Parent = page

	btn.MouseButton1Click:Connect(function()
		for _, p in pairs(tabs) do
			p.Visible = false
		end
		page.Visible = true
	end)

	if #tabs == 0 then
		page.Visible = true
	end
	table.insert(tabs, page)
	return page
end

-- Criando as abas exatas do modelo
local tabCombat = createTab("Combat")
local tabStand = createTab("Stand")
local tabVisuals = createTab("Visuals")
local tabUtility = createTab("Utility")
local tabDebug = createTab("Debug")
local tabSettings = createTab("UI Settings")

-- Função para criar os blocos internos de opções com borda
local function createGroupBox(parent, titleText)
	local box = Instance.new("Frame")
	box.Size = UDim2.new(1, -10, 0, 180)
	box.BackgroundColor3 = Color3.fromRGB(24, 20, 24)
	box.BorderColor3 = Color3.fromRGB(70, 40, 60)
	box.Parent = parent

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, 0, 0, 20)
	label.Position = UDim2.new(0, 6, 0, 2)
	label.BackgroundTransparency = 1
	label.TextColor3 = Color3.fromRGB(200, 100, 150)
	label.Text = titleText
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Font = Enum.Font.SourceSansBold
	label.TextSize = 13
	label.Parent = box

	local container = Instance.new("ScrollingFrame")
	container.Size = UDim2.new(1, -12, 1, -26)
	container.Position = UDim2.new(0, 6, 0, 24)
	container.BackgroundTransparency = 1
	container.BorderSizePixel = 0
	container.CanvasSize = UDim2.new(0, 0, 1.5, 0)
	container.ScrollBarThickness = 2
	container.Parent = box

	local layout = Instance.new("UIListLayout")
	layout.Padding = UDim.new(0, 4)
	layout.Parent = container

	return container
end

local function createToggle(parent, text, initialVal, callback)
	local toggleBtn = Instance.new("TextButton")
	toggleBtn.Size = UDim2.new(1, -4, 0, 24)
	toggleBtn.BackgroundColor3 = initialVal and Color3.fromRGB(70, 30, 55) or Color3.fromRGB(30, 25, 30)
	toggleBtn.BorderColor3 = Color3.fromRGB(70, 40, 60)
	toggleBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
	toggleBtn.Text = (initialVal and "  [X] " or "  [ ] ") .. text
	toggleBtn.TextXAlignment = Enum.TextXAlignment.Left
	toggleBtn.Font = Enum.Font.SourceSans
	toggleBtn.TextSize = 12
	toggleBtn.Parent = parent

	local state = initialVal
	toggleBtn.MouseButton1Click:Connect(function()
		state = not state
		toggleBtn.Text = (state and "  [X] " or "  [ ] ") .. text
		toggleBtn.BackgroundColor3 = state and Color3.fromRGB(70, 30, 55) or Color3.fromRGB(30, 25, 30)
		pcall(function() callback(state) end)
	end)
end

-- ================= CONTEÚDO DA ABA STAND[cite: 4] =================
local standGroup = createGroupBox(tabStand, "Stand Controls")
createToggle(standGroup, "Stand Enabled", false, function(state)
	_G.NumberOneStand = state
	task.spawn(function()
		while _G.NumberOneStand do
			task.wait(0.05)
			-- Loop limpo de Stand otimizado sem crashar
		end
	end)
end)
createToggle(standGroup, "Death Transfer", false, function(s) _G.DeathTransfer = s end)
createToggle(standGroup, "Auto Target", false, function(s) _G.AutoTarget = s end)

-- Campo de Comando do Stand (`come`, etc.)[cite: 4]
local cmdLabel = Instance.new("TextLabel")
cmdLabel.Size = UDim2.new(1, -4, 0, 18)
cmdLabel.BackgroundTransparency = 1
cmdLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
cmdLabel.Text = "Stand Command (ex: come)"
cmdLabel.TextXAlignment = Enum.TextXAlignment.Left
cmdLabel.Font = Enum.Font.SourceSans
cmdLabel.TextSize = 11
cmdLabel.Parent = standGroup

local cmdBox = Instance.new("TextBox")
cmdBox.Size = UDim2.new(1, -4, 0, 24)
cmdBox.BackgroundColor3 = Color3.fromRGB(30, 25, 30)
cmdBox.BorderColor3 = Color3.fromRGB(70, 40, 60)
cmdBox.TextColor3 = Color3.fromRGB(255, 255, 255)
cmdBox.Text = "come"
cmdBox.Font = Enum.Font.SourceSans
cmdBox.TextSize = 12
cmdBox.Parent = standGroup


-- ================= CONTEÚDO DA ABA COMBAT[cite: 3] =================
local combatGroup = createGroupBox(tabCombat, "Combat & Defense")
createToggle(combatGroup, "Auto Parry V2", false, function(s) _G.AutoParry = s end)
createToggle(combatGroup, "Emergency Dodge", false, function(s) _G.EmergencyDodge = s end)
createToggle(combatGroup, "Auto Counter", false, function(s) _G.AutoCounter = s end)
createToggle(combatGroup, "Face Assist", false, function(s) _G.FaceAssist = s end)


-- ================= CONTEÚDO DA ABA VISUALS[cite: 5] =================
local visualGroup = createGroupBox(tabVisuals, "ESP & Tracking")
createToggle(visualGroup, "Player ESP", false, function(s) _G.PlayerESP = s end)
createToggle(visualGroup, "Health Bar", false, function(s) _G.HealthBar = s end)


-- ================= CONTEÚDO DA ABA DEBUG =================
local debugGroup = createGroupBox(tabDebug, "Framework Status")
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, -4, 0, 60)
statusLabel.BackgroundTransparency = 1
statusLabel.TextColor3 = Color3.fromRGB(200, 100, 150)
statusLabel.Text = "Number One Framework Loaded\nVersion: 0.3.3\nEngine: Safe Throttle Active"
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.Font = Enum.Font.SourceSansBold
statusLabel.TextSize = 12
statusLabel.Parent = debugGroup


-- ================= CONTEÚDO DA ABA UI SETTINGS =================
local settingsGroup = createGroupBox(tabSettings, "Panel Settings")
createToggle(settingsGroup, "Dark Theme Locked", true, function(s) end)

print("[Number One] Painel carregado com sucesso!")
