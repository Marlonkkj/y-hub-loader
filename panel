--// Y Hub Alt - Código Completo Ultra-Otimizado (Anti-Lag Extremo + Loader)
pcall(function()
	if setfpscap then
		setfpscap(30) -- Trava o FPS da alt para poupar o processador
	end
end)

-- 1. Limpeza gráfica profunda do ambiente e remoção de partículas globais
pcall(function()
	local Lighting = game:GetService("Lighting")
	Lighting.GlobalShadows = false
	Lighting.FogEnd = 9e9
	Lighting.Technology = Enum.Technology.Compatibility
	
	for _, v in ipairs(Lighting:GetChildren()) do
		if v:IsA("PostEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") then
			v.Enabled = false
		end
	end

	for _, v in ipairs(workspace:GetDescendants()) do
		if v:IsA("BasePart") then
			v.Material = Enum.Material.SmoothPlastic
			v.CastShadow = false
		elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") then
			v:Destroy()
		end
	end
end)

-- 2. Anti-Lag Extremo para Servidores Cheios (Oculta e simplifica players distantes)
pcall(function()
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local RunService = game:GetService("RunService")

	local counter = 0
	RunService.Heartbeat:Connect(function()
		counter = counter + 1
		-- Executa a cada 30 frames para não consumir CPU à toa
		if counter % 30 ~= 0 then return end

		pcall(function()
			local localRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
			if not localRoot then return end

			for _, player in ipairs(Players:GetPlayers()) do
				if player ~= LocalPlayer and player.Character then
					local char = player.Character
					local root = char:FindFirstChild("HumanoidRootPart")
					
					if root then
						local distance = (root.Position - localRoot.Position).Magnitude
						-- Se o player estiver a mais de 30 studs, remove roupas e texturas pesadas dele
						if distance > 30 then
							for _, part in ipairs(char:GetDescendants()) do
								if part:IsA("BasePart") then
									part.Transparency = 1
									part.CanCollide = false
								elseif part:IsA("Accessory") or part:IsA("Clothing") then
									part:Destroy()
								end
							end
						else
							-- Restaura se aproximar
							for _, part in ipairs(char:GetDescendants()) do
								if part:IsA("BasePart") and part.Name == "HumanoidRootPart" then
									part.Transparency = 1
								elseif part:IsA("BasePart") and part.Transparency == 1 then
									part.Transparency = 0
								end
							end
						end
					end
				end
			end
		end)
	end)
end)

-- 3. Coletor de lixo agressivo contínuo
pcall(function()
	collectgarbage("setpause", 130)
	collectgarbage("setstepmul", 250)
	
	task.spawn(function()
		while task.wait(3) do
			collectgarbage("step", 200)
		end
	end)
end)

-- 4. Carregamento seguro do loader oficial atualizado
local success, err = pcall(function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/GodErste/Y-Core/refs/heads/main/loader.lua"))()
end)

if not success then
	warn("[Y Hub Alt] Falha ao carregar: " .. tostring(err))
else
	print("[Y Hub Alt] Injetado com sucesso no modo Ultra-Performance!")
end
