--// Alt Account Optimizer & Command Executor - Modo Anti-Trava
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

pcall(function()
	if setfpscap then
		setfpscap(20) -- Trava o FPS da alt para liberar recursos do PC
	end
end)

--// 1. Limpeza total de gráficos e elementos pesados na Alt para evitar crash
pcall(function()
	local Lighting = game:GetService("Lighting")
	Lighting.GlobalShadows = false
	Lighting.FogEnd = 9e9
	Lighting.Technology = Enum.Technology.Compatibility
	
	for _, v in ipairs(Lighting:GetChildren()) do
		if v:IsA("PostEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") then
			v.Enabled = false
		end
	end

	for _, v in ipairs(workspace:GetDescendants()) do
		if v:IsA("BasePart") then
			v.Material = Enum.Material.SmoothPlastic
			v.CastShadow = false
		elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") or v:IsA("Fire") or v:IsA("Smoke") then
			v:Destroy()
		end
	end
end)

--// 2. Coletor de lixo agressivo para a Alt não acumular memória
pcall(function()
	collectgarbage("setpause", 150)
	collectgarbage("setstepmul", 200)
	
	task.spawn(function()
		while task.wait(5) do
			collectgarbage("step", 150)
		end
	end)
end)

--// 3. Executor de comandos isolado com respiro de thread (Evita o travamento)
task.spawn(function()
	while true do
		-- O 'task.wait(0.05)' ou '0.1' dá um respiro essencial para o motor do Roblox processar 
		-- o comando sem congelar a janela da conta alt.
		pcall(function()
			-- Insira aqui a sua lógica de comando ou chamada da alt de forma segura
		end)
		task.wait(0.08) 
	end
end)

print("[Y Hub Alt] Conta secundária otimizada e blindada contra travamentos.")
